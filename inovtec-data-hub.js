(()=>{
"use strict";
const AGENTS_KEY="kontrol_agents_classeur_v2";
const client=sessionStorage.ivDataHubClient||(sessionStorage.ivDataHubClient="hub_"+Date.now()+"_"+Math.random().toString(16).slice(2));
let user=null,docRef=null,agentRecords=[],chantiers=[],readyAgents=false,readyChantiers=false,unsubDoc=null,unsubSites=null;
const listeners=new Set();
const clone=v=>JSON.parse(JSON.stringify(v));
const parse=s=>{try{return JSON.parse(s)}catch{return null}};
const norm=v=>String(v||"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g," ").trim();
function displayName(a){const i=a?.identity||{};return [i.prenom,i.nom].filter(Boolean).join(" ").trim()||a?.displayName||a?.name||"Agent sans nom"}
function compactAgent(a){return{id:String(a.id||""),createdAt:a.createdAt||new Date().toISOString(),updatedAt:a.updatedAt||new Date().toISOString(),identity:clone(a.identity||{}),job:clone(a.job||{}),displayName:displayName(a),source:"classeur"}}
function splitName(label){const p=String(label||"").trim().split(/\s+/).filter(Boolean);return{prenom:p.shift()||"",nom:p.join(" ")}}
function publicAgents(){return agentRecords.filter(a=>a&&a.id).map(a=>({...compactAgent(a),name:displayName(a)})).sort((a,b)=>displayName(a).localeCompare(displayName(b),"fr",{sensitivity:"base"}))}
function publicSites(){return chantiers.slice().sort((a,b)=>String(a.nom||a.adresse||"").localeCompare(String(b.nom||b.adresse||""),"fr",{sensitivity:"base",numeric:true}))}
function emit(){const detail={agents:publicAgents(),chantiers:publicSites(),readyAgents,readyChantiers};listeners.forEach(fn=>{try{fn(detail)}catch{}});try{window.dispatchEvent(new CustomEvent("inovtec:datahub",{detail}))}catch{}}
function fallbackAgents(){const x=parse(localStorage.getItem(AGENTS_KEY)||"[]");return Array.isArray(x)?x:[]}
function restoreLocalBinaries(remote){const local=fallbackAgents();return remote.map(a=>{const la=local.find(x=>x?.id===a?.id);if(!la)return a;const out={...a};out.docs=(a.docs||[]).map(d=>{const ld=(la.docs||[]).find(x=>x?.id===d?.id);return ld?.dataUrl&&!d.dataUrl?{...d,dataUrl:ld.dataUrl}:d});out.incidents=(a.incidents||[]).map(inc=>{const li=(la.incidents||[]).find(x=>x?.id===inc?.id);if(!li)return inc;return{...inc,photos:(inc.photos||[]).map(p=>{const lp=(li.photos||[]).find(x=>x?.id===p?.id);return lp?.dataUrl&&!p.dataUrl?{...p,dataUrl:lp.dataUrl}:p})}});return out})}
function readAgentsFromDoc(data){const entry=data?.moduleSyncV1?.agents,payload=parse(entry?.payload||"");if(Array.isArray(payload))return payload;const refs=Array.isArray(data?.referentialAgents)?data.referentialAgents:null;if(refs)return refs.map(a=>({...a,docs:[],incidents:[]}));return fallbackAgents()}
function start(u){if(unsubDoc){try{unsubDoc()}catch{}unsubDoc=null}if(unsubSites){try{unsubSites()}catch{}unsubSites=null}user=u||null;docRef=null;readyAgents=false;readyChantiers=false;if(!user){agentRecords=fallbackAgents();readyAgents=true;emit();return}docRef=firebase.firestore().collection("kanban").doc(user.uid);unsubDoc=docRef.onSnapshot(s=>{const data=s.exists?(s.data()||{}):{};agentRecords=readAgentsFromDoc(data);readyAgents=true;emit()},e=>{console.warn("DataHub agents",e);agentRecords=fallbackAgents();readyAgents=true;emit()});unsubSites=firebase.firestore().collection("chantiers").onSnapshot(s=>{chantiers=s.docs.map(d=>({id:d.id,...(d.data()||{})})).filter(c=>c._type!=="disciplinePhotoChunk"&&c._hidden!==true);readyChantiers=true;emit()},e=>{console.warn("DataHub chantiers",e);readyChantiers=true;emit()})}
async function writeAgents(next,reason){if(!docRef||!user)throw new Error("Connexion Firebase requise");const arr=clone(next),now=Date.now(),refs=arr.map(compactAgent);await docRef.set({moduleSyncV1:{agents:{payload:JSON.stringify(arr),updatedAtMs:now,client,reason:reason||"data-hub",compact:false,version:2}},referentialAgents:refs,referentialUpdatedAtIso:new Date(now).toISOString()},{merge:true});agentRecords=arr;try{localStorage.setItem(AGENTS_KEY,JSON.stringify(restoreLocalBinaries(arr)))}catch{}readyAgents=true;emit();return true}
async function createAgent(label){const n=splitName(label),now=new Date().toISOString(),a={id:"agent_"+Math.random().toString(16).slice(2)+"_"+Date.now().toString(16),createdAt:now,updatedAt:now,identity:{prenom:n.prenom,nom:n.nom,telephone:"",email:"",adresse:"",dateNaissance:"",secu:"",permis:"",vehicule:""},job:{poste:"",typeContrat:"",dateEntree:"",sitePrincipal:"",disponibilites:"",notes:"",planningCopies:2},docs:[],incidents:[]};await writeAgents([...agentRecords,a],"create-agent-from-planning");return compactAgent(a)}
async function renameAgent(id,label){const idx=agentRecords.findIndex(a=>a?.id===id);if(idx<0)return false;const next=clone(agentRecords),n=splitName(label),a=next[idx];a.identity=a.identity||{};a.identity.prenom=n.prenom;a.identity.nom=n.nom;a.updatedAt=new Date().toISOString();await writeAgents(next,"rename-agent-from-planning");return true}
async function setAgentPlanningCopies(id,value){const idx=agentRecords.findIndex(a=>a?.id===id);if(idx<0)return false;const n=Math.max(2,Math.min(10,Math.round(Number(value)||2))),next=clone(agentRecords),a=next[idx];a.job=a.job||{};a.job.planningCopies=n;a.updatedAt=new Date().toISOString();await writeAgents(next,"planning-copies");return n}
async function deleteAgent(id){if(!agentRecords.some(a=>a?.id===id))return false;await writeAgents(agentRecords.filter(a=>a?.id!==id),"delete-agent-from-planning");return true}
function findChantier(value){const n=norm(value);if(!n)return null;return chantiers.find(c=>String(c.id)===String(value)||norm(c.nom)===n||norm(c.adresse)===n)||null}
function subscribe(fn){if(typeof fn!=="function")return()=>{};listeners.add(fn);try{fn({agents:publicAgents(),chantiers:publicSites(),readyAgents,readyChantiers})}catch{}return()=>listeners.delete(fn)}
window.InovtecDataHub={get agents(){return publicAgents()},get chantiers(){return publicSites()},get readyAgents(){return readyAgents},get readyChantiers(){return readyChantiers},displayName,findChantier,subscribe,createAgent,renameAgent,setAgentPlanningCopies,deleteAgent};
if(window.firebase&&window.INOVTEC_FIREBASE_CONFIG){if(!firebase.apps.length)firebase.initializeApp(window.INOVTEC_FIREBASE_CONFIG);firebase.auth().onAuthStateChanged(start)}else{agentRecords=fallbackAgents();readyAgents=true;emit()}
})();
