name: Planning coverage health

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Vérifier le Planning direct au double-clic
        shell: bash
        run: |
          set -euo pipefail
          test -s PLANNINGS.html
          test -s planning-calendar.js
          test -s planning-calendar.css
          grep -q 'planning-calendar.js' PLANNINGS.html
          grep -q 'id="editorPopover"' PLANNINGS.html
          grep -q 'id="edDone"' PLANNINGS.html
          grep -q 'captureCalendarDoubleClick' planning-calendar.js
          grep -q 'draftEvent' planning-calendar.js
          grep -q 'saveEditor' planning-calendar.js
          grep -q 'inovtec-data-hub.js' PLANNINGS.html
          grep -q 'inovtec-cloud-sync-v2.js' PLANNINGS.html
          ! grep -q 'id="addTaskBtn"' PLANNINGS.html
          ! grep -q 'legacyFrame' PLANNINGS.html
          ! grep -q 'inovtec-planning-coverage.js' PLANNINGS.html
          ! grep -q 'planning-replacement.js' PLANNINGS.html
          ! grep -q 'planning-pdf.js' PLANNINGS.html
          ! grep -q 'planning-legal-guard.js' PLANNINGS.html
          node --check planning-calendar.js
          node --check inovtec-cloud-sync-v2.js
