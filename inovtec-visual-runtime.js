(()=>{
"use strict";
const frame=document.getElementById("legacyFrame"),summary=document.getElementById("pageSummary");
if(!frame||!summary)return;
const mode=(new URLSearchParams(location.search).get("mode")||"planning").toLowerCase();
let timer=null;
const txt=(el,fallback="—")=>{const v=(el?.textContent||el?.value||"").trim();return v||fallback};
function doc(){try{return frame.contentDocument||null}catch{return null}}
function kontrolDoc(){try{return doc()?.getElementById("kontrolFrame")?.contentDocument||null}catch{return null}}
function inject(d,m){if(!d?.head||!d.body)return;d.body.classList.add("iv-visual-v2",`iv-mode-${m}`);if(!d.querySelector('link[data-inovtec-v2="1"]')){const l=d.createElement("link");l.rel="stylesheet";l.href="inovtec-visual-v2.css?v=20260814-2";l.dataset.inovtecV2="1";d.head.appendChild(l)}}
function card(icon,label,value,note="",tone=""){return `<article class="iv-summary-card ${tone}"><span class="iv-summary-icon">${icon}</span><span class="iv-summary-copy"><small class="iv-summary-label">${label}</small><strong class="iv-summary-value">${value}</strong><span class="iv-summary-note">${note}</span></span></article>`}
function countText(root,selector){return root?.querySelectorAll(selector)?.length||0}
function planning(d){const title=txt(d.getElementById("title"),"Planning").replace(/^Planning\s*[—-]?\s*/i,"")||"Agent";const week=txt(d.getElementById("weekInfo"));const count=txt(d.getElementById("count"),"0 intervention");let total=0;[...d.querySelectorAll("#rows tr")].forEach(tr=>{const s=tr.cells?.[1]?.textContent||"";const m=s.match(/(\d{2}):(\d{2})\s*[–-]\s*(\d{2}):(\d{2})/);if(m)total+=Math.max(0,(+m[3]*60 + +m[4])-(+m[1]*60 + +m[2]));});const hm=total?`${Math.floor(total/60)} h ${String(total%60).padStart(2,"0")}`:"0 h";return [card("♙","Agent actif",title,"Référentiel partagé"),card("▦","Semaine",week,"Période affichée"),card("✓","Interventions",count,"Planning sélectionné"),card("◷","Volume prévu",hm,"Calcul des horaires")];}
function discipline(d){const all=countText(d,"#recordsWrap tbody tr")||countText(d,"#recordsWrap .record");const rows=[...d.querySelectorAll("#recordsWrap tr")];const open=rows.filter(r=>/ouvert|en cours/i.test(r.textContent||"")).length;const agent=txt(d.getElementById("agent"),"Aucun agent");const sync=txt(d.getElementById("syncStatus"),"Connexion…");return [card("⚑","Dossiers",String(all),"Historique visible"),card("!","À suivre",String(open),open?"Ouverts / en cours":"Aucun dossier actif",open?"warning":""),card("♙","Agent du dossier",agent,"Lié au référentiel"),card("☁","Synchronisation",sync,"Firebase")];}
function infos(d){const count=txt(d.getElementById("count"),"0");const site=txt(d.getElementById("nom"),"Aucun chantier");const agent=txt(d.getElementById("agentNom"),"Non affecté");const sync=txt(d.getElementById("syncStatus"),"Connexion…");return [card("▣","Chantiers",count,"Classés alphabétiquement"),card("⌖","Chantier actif",site,"Fiche sélectionnée"),card("♙","Agent affecté",agent,"Référentiel commun"),card("☁","Synchronisation",sync,"Firebase")];}
function agents(d){const count=txt(d.getElementById("agentCount"),"0");const selected=txt(d.getElementById("selectedChip"),"Aucun");const docs=txt(d.getElementById("docCount"),"0 fichier");const inc=txt(d.getElementById("incCount"),"0 entrée");return [card("♙","Agents",count,"Classeur central"),card("◎","Sélection",selected,"Contexte actif"),card("▤","Documents",docs,"Pièces liées"),card("⚑","Incidents",inc,"Suivi agent")];}
function organisation(d){const tasks=countText(d,"#board .task");const overdue=[...d.querySelectorAll("#board .task")].filter(x=>/retard|échéance/i.test(x.textContent||"")&&x.querySelector(".overdue")).length;const archives=txt(d.getElementById("archiveCount"),"0");const sync=txt(d.getElementById("syncStatus"),"Connexion…");return [card("◎","Tâches actives",String(tasks),"Tableau opérationnel"),card("!","En retard",String(overdue),overdue?"À traiter":"Aucun retard",overdue?"warning":""),card("▤","Archives",archives,"Tâches terminées"),card("☁","Synchronisation",sync,"Firebase")];}
function heures(d){const emp=txt(d.getElementById("employee"),"Agent");const week=txt(d.getElementById("weekTotalTop"),"0:00");const month=txt(d.getElementById("monthTotal"),"0:00");const count=txt(d.getElementById("countLabel"),"0");return [card("♙","Agent",emp,"Sélection actuelle"),card("◷","Total semaine",week,"Heures supplémentaires"),card("▦","Total mois",month,"Cumul affiché"),card("≡","Lignes",count,"Entrées filtrées")];}
function temps(d){const a=txt(d.getElementById("timeInput"),"—");const b=txt(d.getElementById("decimalInput"),"—");const r=txt(d.getElementById("result"),"—");return [card("◷","Temps",a,"Heures : minutes"),card("#","Décimal",b,"Heures décimales"),card("⇄","Résultat",r,"Conversion instantanée"),card("✓","Mode","Bidirectionnel","Calcul existant")];}
function salaire(d){const gross=txt(d.getElementById("gross"),"0,00 €");const net=txt(d.getElementById("net"),"0,00 €");const annual=txt(d.getElementById("annual"),"0,00 €");const ratio=txt(d.getElementById("ratio"),"78")+" %";return [card("€","Brut mensuel",gross,"Estimation"),card("✓","Net mensuel",net,"Avant impôt"),card("▦","Brut annuel",annual,"Projection 12 mois"),card("%","Ratio net",ratio,"Paramètre actuel")];}
function kontrol(){const d=kontrolDoc();if(!d)return [card("✓","KONTROL","Chargement…","Contrôle qualité"),card("▦","Tâches","—","Critères existants"),card("◎","Score","—","Note qualité"),card("▤","Photos","—","Pièces du contrôle")];inject(d,"kontrol");const cat=txt(d.getElementById("activeCategoryPill"),"Résidence").replace(/^Actif\s*:\s*/i,"");const tasks=txt(d.getElementById("tasksCount"),"0 tâche");const score=txt(d.getElementById("score"),"—");const photos=txt(d.getElementById("photosCount"),"0 photo");return [card("✓","Catégorie",cat,"Contrôle actuel"),card("▦","Critères",tasks,"Liste inchangée"),card("◎","Note qualité",score,"Calcul KONTROL"),card("▤","Photos",photos,"Éléments du contrôle")];}
function values(){const d=doc();if(!d)return[];inject(d,mode==="kontrol"?"kontrol-cloud":mode);if(mode==="planning")return planning(d);if(mode==="discipline")return discipline(d);if(mode==="infos")return infos(d);if(mode==="agents")return agents(d);if(mode==="organisation")return organisation(d);if(mode==="heures")return heures(d);if(mode==="temps")return temps(d);if(mode==="salaire")return salaire(d);if(mode==="kontrol")return kontrol();return[];}
function render(){const items=values();if(items.length)summary.innerHTML=items.join("");}
function start(){clearInterval(timer);setTimeout(render,90);setTimeout(render,500);setTimeout(render,1200);timer=setInterval(render,1800)}
frame.addEventListener("load",start);if(frame.contentDocument?.readyState==="complete")start();
})();
