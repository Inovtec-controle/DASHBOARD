<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Accueil – Mes applications</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
        sans-serif;
    }

    body {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      background: #0f172a;
      color: #e5e7eb;
    }

    header {
      padding: 1rem 1.5rem;
      background: #020617;
      border-bottom: 1px solid #1f2937;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
    }

    header h1 {
      font-size: 1.2rem;
      font-weight: 600;
    }

    header span {
      font-size: 0.8rem;
      color: #9ca3af;
    }

    main {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 1rem;
      padding: 1rem;
    }

    /* Menu des apps */
    .app-menu {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
    }

    .app-menu button {
      border: 1px solid #22c55e;
      background: transparent;
      color: #e5e7eb;
      padding: 0.5rem 0.9rem;
      border-radius: 999px;
      font-size: 0.9rem;
      cursor: pointer;
      transition: background 0.2s, color 0.2s, transform 0.1s, box-shadow 0.1s,
        border-color 0.2s;
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      white-space: nowrap;
    }

    .app-menu button::before {
      content: "●";
      font-size: 0.6rem;
      opacity: 0.7;
    }

    .app-menu button:hover {
      background: #22c55e;
      color: #022c22;
      transform: translateY(-1px);
      box-shadow: 0 6px 12px rgba(34, 197, 94, 0.3);
    }

    .app-menu button.active {
      background: #22c55e;
      color: #022c22;
      border-color: #4ade80;
      box-shadow: 0 6px 12px rgba(34, 197, 94, 0.4);
    }

    /* Zone d'affichage des apps */
    .apps-wrapper {
      flex: 1;
      min-height: 300px;
      background: #020617;
      border-radius: 1rem;
      border: 1px solid #1f2937;
      padding: 0.8rem;
      overflow: hidden;
    }

    .app {
      display: none;
      width: 100%;
      height: 100%;
      background: #020617;
      border-radius: 0.75rem;
      overflow: auto;
      padding: 0.5rem;
    }

    .app.active {
      display: block;
    }

    .app-placeholder {
      text-align: center;
      margin-top: 3rem;
      color: #6b7280;
      font-size: 0.9rem;
    }

    .app-placeholder strong {
      display: block;
      margin-bottom: 0.5rem;
      color: #e5e7eb;
    }

    /* Pour que les apps collées prennent toute la place */
    .app iframe,
    .app > div,
    .app > section {
      width: 100%;
    }

    @media (min-width: 768px) {
      main {
        padding: 1.5rem 2rem;
      }
    }
  </style>
</head>
<body>
  <header>
    <div>
      <h1>Tableau de bord – Mes applications</h1>
      <span>Clique sur un bouton pour ouvrir l'application correspondante</span>
    </div>
  </header>

  <main>
    <!-- MENU DES APPS -->
    <nav class="app-menu">
      <button type="button" data-target="app-1" class="active">
        Gestion des chantiers
      </button>
      <button type="button" data-target="app-2">
        Conversion temps décimal
      </button>
      <button type="button" data-target="app-3">
        Autre application
      </button>
      <!-- Ajoute autant de boutons que tu veux en suivant le même modèle -->
    </nav>

    <!-- ZONE D'AFFICHAGE DES APPS -->
    <section class="apps-wrapper">
      <!-- APP 1 -->
      <div id="app-1" class="app active">
        <div class="app-placeholder">
          <strong>Application 1 – Gestion des chantiers</strong>
          Colle ici le code HTML / JS / CSS de ton application de gestion
          des chantiers (sans le <code>&lt;!DOCTYPE html&gt;</code>, ni
          <code>&lt;html&gt;</code>, <code>&lt;head&gt;</code>,
          <code>&lt;body&gt;</code>).
        </div>

        <!-- ⚠️ Exemple :
        <style>
          ... ton style spécifique à l'app 1 ...
        </style>

        <script>
          ... ton script spécifique à l'app 1 ...
        </script>

        <div>
          ... ton HTML de l'app 1 ...
        </div>
        -->
      </div>

      <!-- APP 2 -->
      <div id="app-2" class="app">
        <div class="app-placeholder">
          <strong>Application 2 – Conversion temps décimal</strong>
          Colle ici le code de ton application de conversion.
        </div>
      </div>

      <!-- APP 3 -->
      <div id="app-3" class="app">
        <div class="app-placeholder">
          <strong>Application 3</strong>
          Colle ici le code de ta troisième application.
        </div>
      </div>
    </section>
  </main>

  <script>
    // Gestion du changement d'application au clic sur les boutons
    const buttons = document.querySelectorAll(".app-menu button");
    const apps = document.querySelectorAll(".app");

    buttons.forEach((btn) => {
      btn.addEventListener("click", () => {
        const targetId = btn.dataset.target;

        // Active / désactive les boutons
        buttons.forEach((b) => b.classList.remove("active"));
        btn.classList.add("active");

        // Affiche / cache les apps
        apps.forEach((app) => {
          if (app.id === targetId) {
            app.classList.add("active");
          } else {
            app.classList.remove("active");
          }
        });
      });
    });
  </script>
</body>
</html>
